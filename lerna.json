{
  "packages": ["images/*"],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "version": {
      "allowBranch": ["add-publish-image"],
      "ignoreChanges": ["**/*.md"]
    }
  }
}
