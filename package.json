{
  "name": "docker-images",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "print-new-version": "lerna version --conventional-commits"
  }
}
