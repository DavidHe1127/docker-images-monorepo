{
  "name": "docker-images",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": [
    "images/*"
  ],
  "scripts": {
    "set-new-version": "lerna version --conventional-commits",
    "set-new-version:ci": "lerna version --conventional-commits --yes",
    "changed": "lerna changed --json"
  }
}
