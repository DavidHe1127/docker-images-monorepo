{
  "name": "docker-images",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": [
    "images/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish-image": "lerna run publish-image",
    "set-new-version": "lerna run set-new-version",
    "set-new-version:ci": "lerna version --conventional-commits --yes"
  }
}
